---
tags: [csharp, delegates, action]
aliases: [Delegates, Action]
---
# Delegates and Action

A **Delegate** in C# is a type that safely encapsulates a method. Think of it as a variable that holds a function instead of data.

`Action` is a specific, built-in delegate type in C# that represents a method that takes no arguments and returns no value (`void`). 

*(This concept is currently a stub. Refer to the Knowledge Backlog for further investigation on how this compares to constructs in other languages like JavaScript callbacks or Python first-class functions).*
